export default function Post({post}){
    return(
        <div>
            <p>post id: {post.id}</p>
            <p>post body: {post.title}</p>
            <p>post title: {post.body}</p>
            
            
        </div>
    )
}